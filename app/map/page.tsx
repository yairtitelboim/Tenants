'use client';

import PMap from '@/components/pMap';

export default function MapPage() {
  return (
    // Debug color added to visualize the container
    <main className="w-full h-screen bg-green-200">
      <PMap />
    </main>
  );
} 